[project]
name = "llm-red-team"
version = "0.1.0"
description = "Hackathon workspace for agent memory red-team experiments."
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.23.0",
    "pyyaml>=6.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
